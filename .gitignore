
# Дані власного сервера (реальна база з персональними даними)
arm-data/

# Playwright для рендер-тесту (tests/render-check.js) — ставиться на вимогу,
# у репозиторії не зберігається. Проєкт лишається без залежностей.
node_modules/
package-lock.json
